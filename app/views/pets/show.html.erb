<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= @pet.title %> - FureverHome</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <%= stylesheet_link_tag 'main', media: 'all', 'data-turbo-track': 'reload' %>
</head>
<body>

<h1 class="text-center my-4"><%= @pet.title %></h1>
<div class="container" style="max-width: 600px;">
  <div class="card mb-4" style="background-color: #f5f5dc; border: none; border-radius: 15px;">
    <div class="card-body" style="padding: 2rem;">
      <h5 class="card-title" style="color: #8b4513;">Pet Details</h5>
      <p class="card-text">
        <strong>Description:</strong> <%= @pet.description %>
      </p>
      <p class="card-text">
        <strong>Breed:</strong> <%= @pet.breed %>
      </p>
      <p class="card-text">
        <strong>Age:</strong> <%= @pet.age %> years old
      </p>
      <p class="card-text">
        <strong>Status:</strong> <%= @pet.status.humanize %>
      </p>

      <div class="mb-3">
        <strong>Image:</strong><br>
        <div class="mt-3 mb-3" style="text-align: center;">
          <% if @pet.image_url.present? %>
            <%= image_tag @pet.image_url, alt: @pet.title, class: 'img-fluid', style: "height: 360px; object-fit: cover; border-radius: 25px;" %>
          <% else %>
            <%= image_tag 'default_image.png', alt: 'Default Image', class: 'img-fluid', style: "max-height: 300px; object-fit: cover; border-radius: 25px;" %>
          <% end %>
        </div>
      </div>

      <div class="d-flex justify-content-between mt-4">
        <%= link_to 'Edit', edit_pet_path(@pet), class: 'btn btn-primary', style: "background-color: #c5a880; border: none;" %>
        <%= link_to 'Back', pets_path, class: 'btn btn-secondary', style: "background-color: #c5a880; border: none;" %>
      </div>
    </div>
  </div>
</div>

<!-- Include jQuery, Popper.js, and Bootstrap JS -->
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
