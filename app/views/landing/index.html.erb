<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FureverHome</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <%= stylesheet_link_tag 'main', media: 'all', 'data-turbo-track': 'reload' %>
</head>
<body>

<!-- Hero Section -->
<section class="hero-section">
  <div class="hero-content">
    <h1>Help Us Find Forever Homes for Pets</h1>
    <p>Looking to adopt a pet or rehome your furry friend? Start your journey with us today.</p>
    <a href="<%= pets_path %>" class="furever-button">Get Started</a>
  </div>
</section>

<section class="featured-pets mt-5">
  <div class="container">
    <h2 class="text-center">Featured Pets</h2>
    <div class="row">
      <% @featured_pets.each do |pet| %> <!-- Iterate over 4 featured pets -->
        <div class="col-md-3 mb-4">
          <%= link_to pet_path(pet), class: 'card-link' do %>
            <div class="card">
              <%= image_tag pet.display_image_url.presence || asset_path('default_image.png'), class: 'card-img-top', alt: pet.title %>
              <div class="card-body">
                <h5 class="card-title"><%= pet.title %></h5>
                <p class="card-text">Age: <%= pet.age || 'Unknown' %> | Breed: <%= pet.breed || 'Unknown' %></p>
                <p class="card-text"><%= truncate(pet.description, length: 60) %></p> <!-- Truncated description -->
                <p class="card-text"><%= pet.location %></p>
              </div>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</section>

<section class="section" id="why-adopt">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-md-6 hovereffect">
                <%= image_tag asset_path('adopt1.png'), alt: "Adopt a Pet", class: "img-fluid" %>
            </div> <!-- Close the image column -->
            <div class="col-md-6 text-left mt-4">
                <h2>Ready to Adopt?</h2>
                <p>
                    Adopting a pet is a life-changing experience. You'll give a loving animal a new home, and in return, you'll gain a loyal companion.
                    Start your adoption journey and bring home a new friend today!
                </p>
                <a href="<%= pets_path %>" class="furever-button">Browse Pets</a> <!-- Button linking to pets index page -->
            </div>
        </div> <!-- Close the row -->
    </div>
</section>


<section class="section" id="list-a-pet">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-md-6 text-left mt-4">
                <h2>Have a Pet to Rehome?</h2>
                <p>
                    Give your pet the chance to find a loving forever home. It's quick and easy to list your pet, or sign up to become an owner.
                </p>
                <a href="<%= new_pet_path %>" class="furever-button">List a Pet</a> <!-- Button to list a pet -->
            </div>
            <div class="col-md-6 hovereffect">
              <%= image_tag asset_path('adopt2.png'), alt: "List Your Pet", class: "img-fluid" %>
            </div>
        </div>
    </div>
</section>


</body>
</html>
